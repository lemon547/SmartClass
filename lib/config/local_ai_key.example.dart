/// Copy to [local_ai_key.dart] (gitignored) for local seeding.
/// Prefer putting the key in SharedPreferences via「我的 → AI 助手」.
const String kLocalDeepSeekApiKey = '';

/// Optional: SiliconFlow key for voice STT on devices without Google.
const String kLocalSiliconFlowApiKey = '';
